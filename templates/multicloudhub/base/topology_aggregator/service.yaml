apiVersion: v1
kind: Service
metadata:
  name: mcm-topology-aggregator
  labels:
    app: mcm-topology-aggregator
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: https
  selector:
    app: mcm-topology-aggregator
