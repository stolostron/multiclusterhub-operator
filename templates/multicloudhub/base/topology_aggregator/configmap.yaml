apiVersion: v1
kind: ConfigMap
metadata:
  name: topology-aggregator
  labels:
    config: mcm-aggregator
    app: topology-aggregator
data:
  service: "topology-aggregator"
  port: "4000"
  path: "/data/cluster/"
  sub-resource: "/topology"
  use-id: "true"
  secret: "topology-aggregator-secret"
