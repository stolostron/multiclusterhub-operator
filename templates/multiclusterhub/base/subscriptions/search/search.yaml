apiVersion: app.ibm.com/v1alpha1
kind: Subscription
metadata:
  name: search-sub
spec:
  channel: charts-v1
  name: search-prod
  placement:
    local: true 
  packageOverrides:
    - packageName: search-prod
      packageOverrides:
      - path: spec.values
        value: |
          imageTagPostfix: "{{SUFFIX}}"
          global:
            pullSecret: "{{PULLSECRET}}"
          search:
            aggregator:
              image:
                repository: "{{IMAGEREPO}}"
                pullPolicy: "{{PULLPOLICY}}"
            collector:
              image:
                repository: "{{IMAGEREPO}}"
                pullPolicy: "{{PULLPOLICY}}"
            searchapi:
              image:
                repository: "{{IMAGEREPO}}"
                pullPolicy: "{{PULLPOLICY}}"
            redisgraph:
              image:
                repository: "{{IMAGEREPO}}"
                pullPolicy: "{{PULLPOLICY}}"
            operator: 
              image:
                repository: "{{IMAGEREPO}}"
                pullPolicy: "{{PULLPOLICY}}"

