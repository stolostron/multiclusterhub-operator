apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: collectorconfigs.search.open-cluster-management.io
spec:
  group: search.open-cluster-management.io
  names:
    kind: CollectorConfig
    listKind: CollectorConfigList
    plural: collectorconfigs
    shortNames:
    - sccc
    singular: collectorconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CollectorConfig is the schema for the collector-configs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CollectorConfigSpec defines the configuration changes made
              to the resources and fields indexed by Search Collectors.
            properties:
              collectNamespaces:
                description: Specifies the namespaces where resources are to be indexed
                  by Search Collectors
                properties:
                  namespaceSelector:
                    description: |-
                      NamespaceSelector determines namespaces on the managed cluster from which to collect resources.
                      The include and exclude parameters accept file path expressions to include and exclude namespaces by name.
                      The matchExpressions and matchLabels parameters specify namespaces to include by label.
                      See the Kubernetes labels and selectors documentation.
                      The resulting list is compiled by using the intersection of results from all parameters.
                      You must provide either include or at least one of matchExpressions or matchLabels to retrieve namespaces.
                    properties:
                      exclude:
                        description: '[NOT IMPLEMENTED] Exclude is an array of filepath
                          expressions to exclude objects by name.'
                        items:
                          minLength: 1
                          type: string
                        type: array
                      include:
                        description: '[NOT IMPLEMENTED] Include is an array of filepath
                          expressions to include objects by name.'
                        items:
                          minLength: 1
                          type: string
                        type: array
                      matchExpressions:
                        description: '[NOT IMPLEMENTED] MatchExpressions is an array
                          of label selector requirements matching objects by label.'
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: '[NOT IMPLEMENTED] MatchLabels is a map of {key,value}
                          pairs matching objects by label.'
                        type: object
                    type: object
                type: object
              collectionRules:
                description: Defines a list of rules for collecting resources and
                  specific fields.
                items:
                  description: CollectionRule defines a rule for collecting resources
                    and specific fields.
                  properties:
                    action:
                      description: Defines whether to Include (collect) or Exclude
                        (deny collection) resources matching the selector.
                      enum:
                      - include
                      - exclude
                      type: string
                    collectAdditionalPrinterColumnsPriority:
                      description: '[NOT IMPLEMENTED] Specifies to collect additionalPrinterColumns
                        from the CRD with the specified priority or higher.'
                      type: integer
                    collectAnnotations:
                      description: '[NOT IMPLEMENTED] Specifies to collect annotations
                        for the resource.'
                      type: boolean
                    collectConditions:
                      description: '[NOT IMPLEMENTED] Specifies to collect status
                        conditions for the resource.'
                      type: boolean
                    fieldSuffix:
                      default: ""
                      description: |-
                        FieldSuffix appended to custom field names in this rule to avoid collisions with built-in fields.
                        For example, if fieldSuffix is "grc", a field named "status" becomes "status.grc".
                        Defaults to "" (no suffix). If a collision is detected, the built-in field takes precedence.
                        This allows different extensions (GRC, Virt, etc.) to use different suffixes in the same config.
                      type: string
                    fields:
                      description: Specifies the additional fields on the resource
                        to index by Search Collectors.
                      items:
                        description: Field specifies an additional field on the resource
                          to index by Search Collectors.
                        properties:
                          jsonPath:
                            description: JSONPath to the field on the resource.
                            type: string
                          name:
                            description: Specifies the name of the collected item
                              on the resource.
                            type: string
                          type:
                            default: string
                            description: Data type of resource field to be indexed
                              by Search Collectors. Default is a string.
                            enum:
                            - bytes
                            - slice
                            - string
                            - number
                            - mapString
                            type: string
                        required:
                        - jsonPath
                        - name
                        type: object
                      type: array
                    resourceSelector:
                      description: Specifies which resources this rule applies to.
                      properties:
                        apiGroups:
                          description: Specifies apiGroups of resources.
                          items:
                            type: string
                          type: array
                        kinds:
                          description: Specifies kinds of resources.
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - kinds
                      type: object
                  required:
                  - action
                  - resourceSelector
                  type: object
                type: array
            type: object
          status:
            description: CollectorConfigStatus defines the observed state of CollectorConfig.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
