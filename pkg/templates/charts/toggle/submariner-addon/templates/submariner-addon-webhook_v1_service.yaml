---
apiVersion: v1
kind: Service
metadata:
  name: submariner-addon-webhook
  namespace: '{{ default "open-cluster-management" .Values.global.namespace }}'
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: submariner-addon-webhook-cert
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    app: submariner-addon
